# Pipeline-specific Environment Variables
# Copy this file to .env and customize with your settings
# This file can override settings from the root .env file

# =============================================================================
# Database Configuration
# =============================================================================
# Neo4j Configuration (Graph Database)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=chatmind123

# Qdrant Configuration (Vector Database)
QDRANT_URL=http://localhost:6335
QDRANT_COLLECTION=chatmind_embeddings

# =============================================================================
# OpenAI Configuration
# =============================================================================
# Inherited from root .env if not set here
# Uncomment and set if you want pipeline-specific OpenAI settings
# OPENAI_API_KEY=your_openai_api_key_here

# =============================================================================
# Pipeline-Specific Settings
# =============================================================================
# Debug mode for pipeline operations
PIPELINE_DEBUG=True

# Logging level for pipeline operations
PIPELINE_LOG_LEVEL=INFO

# =============================================================================
# Optional: Pipeline-Specific Overrides
# =============================================================================
# Uncomment and modify these if you want pipeline-specific settings
# that differ from your root .env file

# Example: Different database instances for pipeline testing
# NEO4J_URI=bolt://localhost:7688
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=test_password
# QDRANT_URL=http://localhost:6334
# QDRANT_COLLECTION=chatmind_test_embeddings

# Example: Pipeline-specific OpenAI configuration
# OPENAI_API_KEY=sk-your-pipeline-specific-key

# Example: Pipeline-specific model settings
# PIPELINE_MODEL_NAME=gpt-4-turbo
# PIPELINE_MAX_TOKENS=4000

# =============================================================================
# Usage Notes:
# =============================================================================
# 1. Copy this file to .env: cp env.example .env
# 2. Modify the values as needed for your environment
# 3. The pipeline will load settings in this order:
#    - Pipeline .env (highest priority - for overrides)
#    - Root .env (fallback)
#    - Default values (lowest priority)
# 4. This file is gitignored for security 